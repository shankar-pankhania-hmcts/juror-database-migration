-- Generated by Ora2Pg, the Oracle database Schema converter, version 24.0
-- Copyright 2000-2023 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=172.17.0.2;sid=xe;port=1521

SET client_encoding TO 'UTF8';
SET search_path = hk,public;

   
   -----------
   --
   -----------
  
   


CREATE OR REPLACE PROCEDURE hk.housekeeping_report_error (p_part_no text, p_owner text) AS $body$
BEGIN

     utl_file.put_line(l_file,'ERROR. owner:'||p_owner||' part_no:'||p_part_no,TRUE);
     utl_file.put_line(l_file,current_setting('housekeeping.l_err_msg')::varchar(200),TRUE);
     utl_file.put_line(l_file,'Stage : '||current_setting('housekeeping.l_error_stage')::varchar(30));

 
   END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE hk.housekeeping_report_error (p_part_no text, p_owner text) FROM PUBLIC;
