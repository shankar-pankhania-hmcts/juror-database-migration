-- Generated by Ora2Pg, the Oracle database Schema converter, version 24.0
-- Copyright 2000-2023 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=172.17.0.2;sid=xe;port=1521

SET client_encoding TO 'UTF8';
SET search_path = hk,public;

    
   -----------
   --
   -----------
 


CREATE OR REPLACE PROCEDURE hk.housekeeping_record_internal_log_start () AS $body$
BEGIN

     SELECT coalesce(MAX(seq_id),1) + 1
     INTO STRICT   l_seq
     FROM   hk_run_log;

   
     INSERT INTO hk_run_log(seq_id
     ,start_time
     )
     VALUES (l_seq
     ,clock_timestamp()
     );

     COMMIT;

   END;

$body$
LANGUAGE PLPGSQL
;
-- REVOKE ALL ON PROCEDURE hk.housekeeping_record_internal_log_start () FROM PUBLIC;
