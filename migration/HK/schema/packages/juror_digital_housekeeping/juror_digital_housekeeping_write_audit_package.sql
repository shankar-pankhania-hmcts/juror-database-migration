-- Generated by Ora2Pg, the Oracle database Schema converter, version 24.0
-- Copyright 2000-2023 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=172.17.0.2;sid=xe;port=1521

SET client_encoding TO 'UTF8';
SET search_path = hk,public;


--
-- dblink wrapper to call function hk.juror_digital_housekeeping_write_audit as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE hk.juror_digital_housekeeping_write_audit (p_juror_no character varying(9), p_completed_date timestamp(0), p_table_deletions text) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'PERFORM juror_digital_housekeeping_write_audit_atx ( ' || quote_nullable(p_juror_no) || ',' || quote_nullable(p_completed_date) || ',' || quote_nullable(p_table_deletions) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;


-- =================================================================================================
-- =================================================================================================1
CREATE OR REPLACE PROCEDURE hk.juror_digital_housekeeping_write_audit_atx (p_juror_no character varying(9), p_completed_date timestamp(0), p_table_deletions text) AS $body$
BEGIN
     INSERT INTO JUROR_DIGITAL.JD_HOUSEKEEPING_AUDIT(juror_number, selected_date, deletion_date, deletion_summary)
     VALUES (p_juror_no, p_completed_date, clock_timestamp(), p_table_deletions);
	
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE hk.juror_digital_housekeeping_write_audit (p_juror_no text, p_completed_date timestamp(0), p_table_deletions text) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE hk.juror_digital_housekeeping_write_audit_atx (p_juror_no text, p_completed_date timestamp(0), p_table_deletions text) FROM PUBLIC;
